// Anything within comments, was done during a lecture on 02/16, with additional notes from 02/23.

#include <iostream>
// #include <sstream>
#include <string>
#include "Point.h"
#include "Item.h"
#include "Interval.h"

using namespace std;
// using namespace PS5

string ToString(/* Point& P */)
{
	/*
	stringstream out;

	cout << "(" << p.x << "," << p.y << ")";
	return out.str();
	*/
}
/*
string ToString(Item& p)
{
	stringstream out;

	cout << p.id << " := " << Tostring(*(p.position));
	return out.str()
}

string ToString(Interval& p)
{
	stringstream out;

	cout << ((p.included[0])?("["):("(")); // Ternary expression (or conditional statement)
	cout << p.endpoints[0] << "," << p.endpoints[1];
	cout << ((p.included[1])?("]"):(")"));
	return out.str();
}

void Prompt1()
{
	Point a;
	a.x = 6;
	a.y = 8;
	cout << ToString(a) << "\n";
}

void Prompt2()
{
	Item b;
	b.id = "Home";
	b.position-> x = 10;
	b.position-> y = 5;
	cout << ToString(b) << "\n";
}

void Prompt3()
{
	Interval c;
	c.endpoints[0] = 54;
	c.endpoints[1] = 73;
	c.included[1] = false;
	cout << ToString(c) << "\n";
}
*/
int main()
{
	/*
	Prompt3();
	*/
	return 0;
}
